allowBuilds:
  # esbuild's postinstall only downloads its own platform-specific binary (needed by tsx, which
  # scripts/hello-sra.ts runs on). unrs-resolver similarly just fetches its native binary for
  # eslint's import resolution. Both are widely-used, low-risk infra packages.
  esbuild: true
  unrs-resolver: true
