// TODO: order status API — SSE endpoint watching OrderPaid via viem watchContractEvent
// (CLAUDE.md §8 "Payment detection"). This is the "backend" for this app: there is no
// separate backend service, it's this Next.js API route.
export {};
